{
  "name": "travis-pg-10-test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "node index.js",
    "bump": "git add . && git commit -m 'Bump' --allow-empty && git push origin master"
  },
  "dependencies": {
    "pg": "^7.8.2"
  }
}
